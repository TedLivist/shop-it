en:
  active_interaction:
    errors:
      models:
        users/auth/sign_in:
          attributes:
            user:
              inactive: "Inactive %{attribute}. Verify OTP or contact admin for account activation"

        delivery_addresses/create:
          attributes:
            is_default:
              must_be_true: "Must be true or left vacant"
        delivery_addresses/update:
          attributes:
            is_default:
              must_be_true: "Must be true or left vacant"

        orders/create:
          attributes:
            stock:
              not_enough: "%{attribute} of %{product_name} is not enough for that quantity"